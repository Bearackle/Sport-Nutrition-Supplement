type TProps = {
  title: string;
  description: Array<string>;
  conditionUrl: string;
  code: string;
};

const CouponCard = ({ title, description, conditionUrl, code }: TProps) => {
  return (
    <div>
      <div></div>
    </div>
  );
};

export default CouponCard;
